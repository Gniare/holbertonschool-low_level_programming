#include <stdio.h>

/**
*int _strlen - this function that returns the length of a string.
*Return lenght
*/

#include <stdio.h>

int _strlen(char *s)

{
int length = 0;
while (*s != '\0')
{
length++;
s++;
}
}
